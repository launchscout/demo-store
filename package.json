{
  "name": "demo-store",
  "version": "0.0.1",
  "description": "A demo store for stripe-cart",
  "main": "index.js",
  "scripts": {
    "start": "eleventy --serve --port 8081",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "eleventy",
    "gh-build": "eleventy --pathprefix 'demo-store'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "eleventy-sass": "^2.1.7",
    "launch-cart": "^0.0.6",
    "live-server": "^1.2.2"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0-beta.3",
    "stripe": "^11.7.0"
  }
}
